[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — the app is client-side routed, so serve index.html for unknown paths
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
